import { describe, it } from 'vitest'

describe('processor', () => {
	it('remark', async () => {
		// const processor = initProcessor()
		// processor.use(remarkHighlight)
		// const file = await processor.process('==`helloAhelloN`==')
		// console.log('data', file.value)
	})
})
